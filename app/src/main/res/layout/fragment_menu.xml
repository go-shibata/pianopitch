<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="fragment"
            type="com.example.go.piano_pitch.ui.menu.MenuFragment" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.menu.MenuFragment">

        <Button
            android:id="@+id/btn_pitch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/button_primary_background"
            android:onClick="@{() -> fragment.onClickPitchButton()}"
            android:padding="@dimen/margin_small"
            android:text="@string/menu_btn_pitch_text"
            android:textColor="@color/colorSecondary"
            app:layout_constraintBottom_toTopOf="@id/btn_piano"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_chainStyle="packed" />

        <Button
            android:id="@+id/btn_piano"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_mid"
            android:background="@drawable/button_primary_background"
            android:onClick="@{() -> fragment.onClickPianoButton()}"
            android:padding="@dimen/margin_small"
            android:text="@string/menu_btn_piano_text"
            android:textColor="@color/colorSecondary"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/btn_pitch" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>